<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.capstone.peradaban.singkat">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Singkat">
        <activity android:name=".status.DetailStatusActivity"></activity>
        <activity android:name=".ajukan.UploadFotoActivity" />
        <activity android:name=".beranda.hubungi.HubungiActivity" />
        <activity android:name=".ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".ajukan.KonfirmasiFormActivity" />
        <activity android:name=".ajukan.PendaftaranActivity" />
        <activity android:name=".ajukan.PhoneAuthActivity" />
        <activity android:name=".ajukan.VerifyAuthActivity" />
        <activity android:name=".beranda.faq.FaqActivity" />
        <activity android:name=".ajukan.KetentuanActivity"/>
    </application>

</manifest>